# GitHub Discussions - seed questions (Sprint 3)

Use these as initial prompts to start community Q&A.

## SDK & MCP questions

1. How do I switch between testnet and mainnet / paid mode in the Python + TypeScript SDKs?
2. What does “server-side watermarking” mean for `POST /v1/certify` and how should I describe it in my product?
3. Which MCP tool should I use to certify vs verify vs detect, and what are the expected inputs/outputs?
4. Can I call `certify_content` with a `content_hash` instead of raw text?
5. How should I handle rate limits / operation quotas when running in free testnet mode?

## Compliance workflow questions

6. What’s a good pattern for an agent that must verify compliance before publishing?
7. How can I store / reuse certificate ids in my application for later verification?
8. How do I attach provenance fields (like disclosure/generation_type/metadata) to `certify` calls?

## Integration & publishing questions

9. How do I build and publish the TypeScript npm package for directory listings?
10. What’s the recommended way to integrate the MCP server with tools like LangChain or Cursor?

